# دليل إعداد Firebase - قولنا يحد

هذا الدليل الشامل سيساعدك في ربط موقع "קולנו יחד - قولنا يحد" بقاعدة بيانات Firebase.

## 📋 المتطلبات

- حساب Google
- المتصفح (Chrome, Firefox, Safari, Edge)
- الملفات التي تم إنشاؤها في هذا المشروع

## 🚀 الخطوة 1: إنشاء مشروع Firebase

### 1.1 الدخول إلى Firebase Console

1. افتح المتصفح واذهب إلى: [https://console.firebase.google.com](https://console.firebase.google.com)
2. سجل الدخول بحساب Google الخاص بك
3. انقر على "إضافة مشروع" أو "Add project"

### 1.2 إعداد المشروع

1. **اسم المشروع**: أدخل اسم المشروع (مثل: `kolana-yahad`)
2. **Google Analytics**: يمكنك تفعيله أو تعطيله (اختياري)
3. انقر على "إنشاء المشروع" أو "Create project"
4. انتظر حتى يتم إنشاء المشروع (قد يستغرق دقيقة)
5. انقر على "متابعة" أو "Continue"

## 🔧 الخطوة 2: إضافة تطبيق ويب

### 2.1 تسجيل التطبيق

1. في صفحة المشروع الرئيسية، انقر على أيقونة الويب `</>`
2. **اسم التطبيق**: أدخل اسم التطبيق (مثل: `Kolana Yahad Website`)
3. **لا تحتاج** لتفعيل Firebase Hosting الآن
4. انقر على "تسجيل التطبيق" أو "Register app"

### 2.2 نسخ مفاتيح التكوين

ستظهر لك شاشة بها كود JavaScript يحتوي على مفاتيح التكوين. انسخ القيم التالية:

```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

**احتفظ بهذه المفاتيح!** ستحتاجها في الخطوة التالية.

## 🔑 الخطوة 3: تحديث مفاتيح التكوين

### 3.1 تحديث ملف firebase-config.js

1. افتح ملف `firebase-config.js` في مجلد المشروع
2. استبدل القيم الموجودة بالمفاتيح التي نسختها:

```javascript
const firebaseConfig = {
    apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXX", // استبدل هذا
    authDomain: "kolana-yahad.firebaseapp.com", // استبدل هذا
    projectId: "kolana-yahad", // استبدل هذا
    storageBucket: "kolana-yahad.appspot.com", // استبدل هذا
    messagingSenderId: "123456789012", // استبدل هذا
    appId: "1:123456789012:web:xxxxxxxxxxxxx" // استبدل هذا
};
```

3. احفظ الملف

## 🔐 الخطوة 4: تفعيل Authentication (المصادقة)

### 4.1 تفعيل تسجيل الدخول بالبريد الإلكتروني

1. في Firebase Console، من القائمة الجانبية، انقر على "Authentication"
2. انقر على "Get started" أو "البدء"
3. انتقل إلى تبويب "Sign-in method"
4. انقر على "Email/Password"
5. **فعّل** الخيار الأول "Email/Password"
6. **لا تحتاج** لتفعيل "Email link (passwordless sign-in)"
7. انقر على "Save" أو "حفظ"

### 4.2 إضافة نطاقات مصرح بها (اختياري)

إذا كنت ستنشر الموقع على GitHub Pages:

1. في نفس صفحة Authentication
2. انتقل إلى تبويب "Settings"
3. في قسم "Authorized domains"
4. انقر على "Add domain"
5. أضف: `USERNAME.github.io` (استبدل USERNAME باسم المستخدم الخاص بك)

## 💾 الخطوة 5: تفعيل Firestore Database

### 5.1 إنشاء قاعدة البيانات

1. من القائمة الجانبية، انقر على "Firestore Database"
2. انقر على "Create database"
3. اختر "Start in **test mode**" (للتطوير)
   - **ملاحظة**: سنقوم بتحديث قواعد الأمان لاحقاً
4. اختر موقع قاعدة البيانات (اختر الأقرب لك، مثل: `europe-west1`)
5. انقر على "Enable"

### 5.2 تحديث قواعد الأمان

بعد إنشاء قاعدة البيانات:

1. انتقل إلى تبويب "Rules"
2. استبدل القواعد الموجودة بالقواعد التالية:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد مجموعة المستخدمين
    match /users/{userId} {
      // السماح للمستخدم بقراءة وكتابة بياناته فقط
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // قواعد مجموعة الرسائل
    match /messages/{messageId} {
      // السماح للجميع بإنشاء رسائل (حتى غير المسجلين)
      allow create: if true;
      // السماح للمستخدمين المسجلين بقراءة الرسائل
      allow read: if request.auth != null;
      // منع التعديل والحذف
      allow update, delete: if false;
    }
  }
}
```

3. انقر على "Publish" أو "نشر"

## ✅ الخطوة 6: اختبار التكامل

### 6.1 اختبار تسجيل حساب جديد

1. افتح ملف `auth.html` في المتصفح
2. انقر على "إنشاء حساب جديد"
3. املأ البيانات:
   - الاسم الكامل
   - البريد الإلكتروني
   - كلمة المرور (6 أحرف على الأقل)
   - تأكيد كلمة المرور
4. انقر على "إنشاء حساب"
5. يجب أن ترى رسالة نجاح وإعادة توجيه للصفحة الرئيسية

### 6.2 التحقق من إنشاء المستخدم

1. ارجع إلى Firebase Console
2. اذهب إلى "Authentication" → "Users"
3. يجب أن ترى المستخدم الجديد في القائمة

### 6.3 اختبار تسجيل الدخول

1. افتح `auth.html` مرة أخرى
2. أدخل البريد الإلكتروني وكلمة المرور
3. انقر على "تسجيل الدخول"
4. يجب أن ترى رسالة نجاح وإعادة توجيه

### 6.4 اختبار نموذج الاتصال

1. افتح `index.html`
2. انتقل إلى قسم "צור קשר" (اتصل بنا)
3. املأ النموذج وأرسل رسالة
4. يجب أن ترى رسالة نجاح

### 6.5 التحقق من حفظ الرسالة

1. ارجع إلى Firebase Console
2. اذهب إلى "Firestore Database"
3. يجب أن ترى مجموعة (collection) جديدة باسم `messages`
4. انقر عليها لرؤية الرسائل المحفوظة

### 6.6 اختبار لوحة التحكم

1. افتح `admin.html`
2. سجل الدخول إذا لم تكن مسجلاً
3. يجب أن ترى جدولاً بجميع الرسائل المرسلة

## 🔒 الخطوة 7: تحسين الأمان (مهم!)

### 7.1 قواعد أمان إضافية

بعد التأكد من عمل كل شيء، يمكنك تحديث قواعد Firestore لمزيد من الأمان:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد المستخدمين
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // قواعد الرسائل مع حدود
    match /messages/{messageId} {
      // السماح بإنشاء رسالة واحدة كل 5 دقائق
      allow create: if request.resource.data.name is string &&
                       request.resource.data.email is string &&
                       request.resource.data.message is string &&
                       request.resource.data.name.size() > 0 &&
                       request.resource.data.email.matches('.*@.*\\..*') &&
                       request.resource.data.message.size() > 10 &&
                       request.resource.data.message.size() < 1000;
      
      allow read: if request.auth != null;
      allow update, delete: if false;
    }
  }
}
```

### 7.2 تقييد النطاقات المصرح بها

1. في Authentication → Settings → Authorized domains
2. احذف أي نطاقات لا تستخدمها
3. أضف فقط النطاقات التي تحتاجها:
   - `localhost` (للتطوير المحلي)
   - `USERNAME.github.io` (لـ GitHub Pages)
   - نطاقك المخصص (إن وجد)

## 📱 الخطوة 8: النشر على GitHub Pages

بعد إكمال إعداد Firebase، يمكنك نشر الموقع:

1. اتبع التعليمات في ملف `README.md` الرئيسي
2. تأكد من أن جميع الملفات محدثة بمفاتيح Firebase الصحيحة
3. ارفع الملفات إلى GitHub
4. فعّل GitHub Pages
5. أضف نطاق GitHub Pages إلى Authorized domains في Firebase

## 🎯 ملخص الملفات المهمة

| الملف | الوصف |
|-------|-------|
| `firebase-config.js` | تكوين Firebase (يحتوي على المفاتيح) |
| `auth.html` | صفحة تسجيل الدخول والتسجيل |
| `auth.js` | منطق المصادقة |
| `admin.html` | لوحة التحكم لعرض الرسائل |
| `admin.js` | منطق لوحة التحكم |
| `index.html` | الصفحة الرئيسية (محدثة) |
| `script.js` | السكريبت الرئيسي (محدث) |
| `style.css` | الأنماط (محدثة) |

## ❓ حل المشاكل الشائعة

### المشكلة: "Firebase: Error (auth/invalid-api-key)"

**الحل**: تأكد من أنك نسخت `apiKey` بشكل صحيح من Firebase Console إلى `firebase-config.js`

### المشكلة: "Firebase: Error (auth/operation-not-allowed)"

**الحل**: تأكد من تفعيل Email/Password في Authentication → Sign-in method

### المشكلة: "Missing or insufficient permissions"

**الحل**: تحقق من قواعد Firestore Security Rules وتأكد من أنها صحيحة

### المشكلة: لا تظهر الرسائل في لوحة التحكم

**الحل**: 
1. تأكد من أنك مسجل دخول
2. تحقق من قواعد Firestore (يجب السماح بالقراءة للمستخدمين المسجلين)
3. افتح Console في المتصفح (F12) وتحقق من الأخطاء

### المشكلة: "auth/unauthorized-domain"

**الحل**: أضف النطاق إلى Authorized domains في Firebase Console

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تحقق من Console المتصفح (F12) للأخطاء
2. راجع [وثائق Firebase](https://firebase.google.com/docs)
3. تأكد من أن جميع الخطوات تم تنفيذها بشكل صحيح

## 🎉 تهانينا!

إذا أكملت جميع الخطوات بنجاح، فإن موقعك الآن:

- ✅ متصل بقاعدة بيانات Firebase
- ✅ يدعم تسجيل المستخدمين وتسجيل الدخول
- ✅ يحفظ رسائل نموذج الاتصال في Firestore
- ✅ لديه لوحة تحكم لعرض الرسائل
- ✅ محمي بقواعد أمان Firebase

---

**ملاحظة مهمة**: لا تشارك مفاتيح Firebase الخاصة بك مع أي شخص، ولا ترفعها إلى repositories عامة بدون حماية مناسبة!
